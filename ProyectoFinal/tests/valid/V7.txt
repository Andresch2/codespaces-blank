// @title: V7 expresión anidada compleja con fracciones (COP)
// @expect: ok
// Unidades: COP (pesos colombianos)
a = 12345;
b = 6789;
c = 2.75;

// Ingreso con varias operaciones anidadas
ingreso = ((a * (b + 111)) / (3 + c)) + ((a - b) * (c + 1.25));

// Tasa como suma de fracciones no evidentes (0.15 + 0.035 = 0.185)
tasa = (3/20) + (7/200);

impuesto = 0;
si ingreso > 100000: impuesto = ingreso * tasa;
print(impuesto);